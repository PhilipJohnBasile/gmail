import styled from 'styled-components';

const EmailsView = () => {
    return (
        <Wrapper>
            emails
        </Wrapper>
    )
}
export default EmailsView

const Wrapper = styled.div`

`
